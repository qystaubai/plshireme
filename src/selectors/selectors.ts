// import {useStore} from "react-redux";
// import {Store} from "redux";
//
// const store = useStore();
// export const getGoalsState = (store: Store) => store.getState()
//
// export const getGoalsList = (store: Store) => {
//     getGoalsState(store) ? getGoalsState(store).goals : []
// }
//
// export const getGoalById = (store: Store, id: number) => (
//     getGoalsState(store) ? { ...getGoalsState(store).goals[id], id} : {}
// )
//
// export const getGoals = (store: Store) => {
//
// }
